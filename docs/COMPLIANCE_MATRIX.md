# MetricRCA — Strict Docs Compliance Matrix (Binding Implementation Gate)

**Base commit** `b6b4b5b` (docs-only). **Current status:** P1 rows 1–10 and 26 are implemented. P2 rows 12, 16(partial), 17, 18 are functionally implemented. Row 12 and Row 27 metadata-hardcode deviations are fixed by `docs/iteration-prompts/fix-001-metadata-hardcode.md` plus the PR review hotfix plan: metric metadata reads through `MetadataRepository`, metadata-only methods do not require LLM provider availability, tool callers receive injected metadata service access, tool SQL execution failures return typed Observations without fabricated Evidence, and `parse_question` delegates to a configured live LLM intent planner boundary. P6 deepagents core rows 11, 13, 14, 19, 20, and 24 are implemented with `RunOrchestrator`, `GuardMiddleware`, persisted-artifact Reflection, and v2 zero-fallback negative tests.

**Authority order** (per `docs/IMPLEMENTATION_CONTRACT.md` §1): current user instruction → `AGENTS.md`/contract → `docs/MetricRCA.md` → `docs/MetricRCA-roadmap-checklist.md` → `docs/deep-research-report.md` (background only).

**This file is the binding gate.** A change is acceptable only if it satisfies the relevant row's *Required behavior* AND its *Tests that must prove it* pass — where each proof test is written so the forbidden shortcut turns it red. Keep the "Current status" understanding updated as phases land; do not weaken a test to fit a shortcut.

## Phase legend (roadmap §9)
P1 Data+Guardrail · P2 Services+Tools · P6 deepagents core · P4 API+UI · P5 Eval+Docs.
Foundation rows (domain models, config) are built at the start of P1 because everything depends on them.

## Process rule (contract §5 / AGENTS.md)
For each row: write the proof test(s) FIRST (encoding the docs), confirm they fail against a shortcut, then implement until green. Final completion runs:
`make seed && make eval && make test && python -W error::ResourceWarning -m unittest discover -s tests -v`.

---

## Compliance Matrix

| # | Requirement | Docs ref | Required files | Required behavior | Tests that must prove it | Phase | Shortcut risk to avoid |
|---|---|---|---|---|---|---|---|
| 1 | Dependencies & pyproject | §20 (1086–1097); roadmap 阶段1; current P1 scope | `pyproject.toml` | Package `metric_rca`, Py3.12; declare only P1 MVP deps with pinned constraints: pydantic>=2, pydantic-settings, sqlalchemy>=2, pymysql, sqlglot, pandas, pytest. FastAPI/uvicorn/httpx/LangGraph/LangChain/scikit-learn and frontend tooling are Phase >1 and must not be declared now. | Clean venv `pip install -e .`, then parse installed metadata: assert exact P1 deps **declared with pinned constraints**; assert Phase >1 deps absent; `pydantic.version_info.major==2`; sqlalchemy>=2. | P1 | Lighter-lib substitution; pydantic v1; deps importable from ambient env but undeclared; pulling Phase >1 deps into P1. |
| 2 | Makefile | §20 (1099–1107); current P1 scope | `Makefile` | `up`=`docker compose up -d mysql`; `seed`=`python -m metric_rca.data.seed_data`; `test`=`pytest -q`; `eval` may exist but must only call the Phase >1 placeholder and must not echo success. No API/UI targets in P1. | Parse Makefile, assert each P1 target == documented command; assert no uvicorn/frontend dev-server target; `make test` runs pytest. | P1 | `echo`/`true` targets; eval printing fake success; API/UI targets pretending P1 completion. |
| 3 | Docker / local DB | §20 (1092,1109); §11 (839–842); current P1 scope | `docker-compose.yml` | Service mysql(8.x, init schema.sql, healthcheck); execution via read-only DB account; statement timeout. No API/UI services in P1. | Parse compose: only mysql service; mysql 8.x; schema.sql init mount; integration connect to seeded DB; DML via exec conn raises. | P1 | SQLite substitution; skipping read-only account; sneaking API/UI services into P1. |
| 4 | schema.sql | §9 (530–727); roadmap §1.2/1.3 | `metric_rca/data/schema.sql` | All 17 tables w/ documented cols/types/keys incl. **fact_customer_ticket**, memory_record, eval_run, eval_case_result; `business_date DATE`, system ts `DATETIME`. | After seed, introspect `information_schema`: all 17 tables w/ documented columns/types; no `fact_ticket`; PKs/indexes present. | P1 | Dropping "optional" system tables; `fact_ticket`; TIMESTAMP/VARCHAR dates. |
| 5 | seed_data deterministic 60-day + 5 GT cases | §10 (745–763); roadmap §4 | `data/seed_data.py`, `data/anomaly_injection.py` | SEED=20260606, business_today=2026-06-06, target=2026-06-05, `gmv_no_anomaly` control date=2026-06-04, 60 days, Asia/Tokyo; weekday/channel/category/campaign/inventory/refund effects; seed metric_definition; inject 4 target-date anomaly cases + 1 no-anomaly control case; write anomaly_ground_truth; idempotent. | Seed twice → identical content hash; 60 distinct business_date; anomaly_ground_truth = exactly 5 documented case_ids w/ correct fields; target-date global GMV is anomalous by §12 formula; `gmv_no_anomaly.expected_anomaly=0` and its control-date global GMV is not anomalous; paid_ads spend/clicks/uv below same-weekday baseline. | P1 | Unseeded RNG; GT answers hardcoded in agent; non-idempotent seed; compensating non-causal segments to hide a target-date global GMV anomaly. |
| 6 | Pydantic / domain models | §2 (70–268); roadmap §3.9 | `domain/enums.py`, `domain/models.py` | All enums + models (TimeRange…AgentRun); every contract model `ConfigDict(extra="forbid")`; v2 validators. | Each model rejects unknown field (ValidationError); enum membership; `model_dump`/`model_validate` round-trip. | P1 | dataclasses/dicts; missing `extra="forbid"`; pydantic v1. |
| 7 | QuerySpec | §2 (145–159); §11; roadmap §3.2 | `guardrails/query_spec.py` (+domain) | metric_id, time_range, group_by(≤2 validator), filters, limit le=5000, purpose enum; `build_query_spec` whitelist-only; never free SQL. | group_by>2 raises; limit>5000 raises; bad purpose raises; non-whitelist dim → DIMENSION_NOT_ALLOWED/QUERY_SPEC_INVALID. | P1 | Raw SQL in QuerySpec; unrestricted group_by. |
| 8 | SQLRenderer | §11 (773–775); roadmap §2.3 | `guardrails/renderer.py` | Deterministic template per (metric, group_by) → parameterized SQL + sha256 hash → SQLPlan; always injects fact `business_date` + LIMIT; no CTE/subquery/`*`; category → whitelist INNER JOIN to dim_product. | gmv current → single SELECT w/ date+LIMIT, no star; category → exact whitelist JOIN; channel → no join; **all 6 question-family renders pass SQLGuard**; hash==sha256. | P1 | Concat allowing column injection; LLM joins; output that its own guard rejects. |
| 9 | sqlglot AST SQLGuard | §11 (777–865); roadmap §5 | `guardrails/sql_guard.py` | AST via `sqlglot.parse`: reject multi-stmt, non-SELECT, DML/DDL/Command, `*`, CTE, subquery/derived, non-whitelist table/col, missing fact table, missing fact business_date, missing LIMIT, non-renderer joins; allow only whitelist INNER JOIN; preserve hash; feed sql_audit. | 9 documented cases + column-whitelist/non-renderer-JOIN/missing-LIMIT rejects; **AST-proof**: comment/case-obfuscated DROP & derived table still rejected (defeats naive regex). | P1 | **P0:** regex guard posing as AST; keyword-only checks; arbitrary joins. |
| 10 | Repository & SQL audit | §4 (343); §11 (839–844); §9 | `repositories/*.py` | Execute ONLY passed SQLPlan via `text()` parameterized, read-only acct, timeout, LIMIT; every SQL → sql_audit row (hash/guard_status/errors/row_count/latency); persist all system tables; never run raw SQL. | Raw/un-guarded SQL raises (`test_sql_guard_rejection_cannot_bypass_renderer`); each fact query writes sql_audit w/ correct hash+row_count; filter values param-bound (injection stays literal); DML raises. | P1→P2 | Direct SQL exec w/o guard; skipping sql_audit; f-string params. |
| 11 | deepagents tool-calling boundary | MetricRCA §6 v2; final-design/01 §2–§4; ADL-0008/0009 | `agent/factory.py`, `agent/middleware.py`, `agent/deep_tools.py`, `agent/prompts.py` | LLM is required and uses deepagents/LangChain free tool-calling inside the registered MetricRCA whitelist only; `write_todos` planning is retained; filesystem tools are disabled/not exposed. For pinned `deepagents==0.3.5`, MetricRCA composes the deepagents core middleware without `FilesystemMiddleware` because `create_deep_agent` has no removal/permissions switch; compiled graph introspection must prove no `ls/read_file/write_file/edit_file/glob/grep/execute` exposure and must hard-fail if deepagents is missing. LLM never writes SQL and sees only structured observations. Tool args schemas are derived from the same registry that exposes tools; no second hand-written schema list. One illegal tool name or args schema violation returns recoverable `ACTION_SCHEMA_INVALID` and does not execute the tool; the second consecutive illegal call to the same tool fails the run. Orchestrator seeds explicit `dimension=value` question scope into middleware so LLM planning remains free but cannot silently switch scoped facts. | `test_llm_unavailable_fails_before_agent_loop`, `test_illegal_tool_args_short_circuit_without_execution`, `test_real_deepagents_compiled_graph_exposes_only_metric_rca_tools`, `test_tool_arg_schema_registry_covers_every_exposed_metric_rca_tool`, `test_single_illegal_args_then_valid_call_continues`, `test_two_consecutive_illegal_args_same_tool_fails_run`, `test_orchestrator_seeds_explicit_question_scope_into_guard_context`. | P6/P7 | **P0:** reintroducing deterministic action-selection policy; exposing filesystem tools; LLM writes SQL; silently rewriting illegal args and continuing; asserting exposure from our own tool list instead of the compiled graph; maintaining a stale schema map separate from exposed tools; skipping the compiled-graph proof when deepagents is absent; prompt-only handling of explicit user scope. |
| 12 | Tool layer | §13 (910–931); contract (114–133) | `agent/tools/{detect_anomaly,drilldown_dimension,fetch_related_signal,calculate_contribution}.py` (+ `services/metric_service.py`) | Each: typed In/Out, QuerySpec→Renderer→Guard→Repo, return Observation+Evidence, write sql_audit, never bypass guard; typed errors. SQL execution/plan/guard failures and system-table Evidence persistence failures become `Observation(ok=False, error_code=...)` with no fabricated Evidence. `fetch_related_signal` enforces deterministic metric/dimension → signal_type policy before querying. `get_metric_definition` / `get_schema_context` must not be hardcoded runtime dictionaries and must not require LLM availability; metadata must come from persisted metadata, schema metadata, or explicit metadata repository. | Each returns Observation; data tools bind guard-passed Evidence; assert SQLPlan.guard_status=passed pre-execute; execute_plan `SQL_EXECUTION_FAILED`/`SQL_PLAN_INVALID` returns typed Observation without Evidence; `create_evidence` `SYSTEM_TABLE_WRITE_FAILED` returns typed Observation without returned Evidence; non-whitelist dim → DIMENSION_NOT_ALLOWED; wrong signal type for metric/dimension → `QUERY_SPEC_INVALID` without query/write; sample_n<3 → INSUFFICIENT_BASELINE_DATA; empty → no attribution; mutate/drop persisted metric metadata through `MetricService` and prove runtime output/error changes; metadata methods work with no OpenAI key while `parse_question` fails `LLM_REQUIRED_UNAVAILABLE`; source scan rejects service-level metric-definition/schema-context constants. | P2 | **P0:** empty placeholder tools; direct DB read; fabricated payload w/o Evidence; raw system-table write exceptions; service constants pretending to be DB-backed metadata; metadata access blocked by LLM provider availability; prompt-only signal_type selection. |
| 13 | RunOrchestrator lifecycle | MetricRCA §5 v2; final-design/01 §1, §3, §5–§6; final-design/03; ADL-0009/0010 | `agent/runner.py`, `agent/factory.py`, `agent/reflection.py`, `reporting/projector.py` | `RunOrchestrator` owns run creation, LLM intent parsing, run target metric/date anchoring before agent invocation, persisted-artifact Reflection, one repair re-entry on the same thread, ADL-0006 report projection, memory/task finalization, and agent_run terminal status. Transient LLM provider errors may continue only after terminal persisted artifacts already exist (`no_anomaly` E1 or complete E4+E_rank chain); the same errors before terminal evidence remain fail-fast. No hand-written `graph.py/state.py/react.py/nodes/` import shim may remain. | `test_successful_run_creates_operation_task_from_report`, `test_terminal_ranked_evidence_allows_transient_llm_error_to_finish_report`, `test_transient_llm_error_without_terminal_evidence_still_fails_fast`, `test_orchestrator_anchors_run_metric_to_parsed_intent_before_agent_loop`, `test_repair_guard_failure_stops_before_report`, `test_reflection_repair_exhausted_no_report`, `test_legacy_graph_modules_removed`. | P6/P7 | **P0:** sequential graph replacement hidden behind `graph.py`; anchoring run metric to first tool call; report after failed Reflection; continuing after nonterminal LLM failure; repair that bypasses middleware/tool path. |
| 14 | GuardMiddleware semantics | MetricRCA §6 v2; final-design/01 §3; final-design/03 §4; ADL-0009/0010 | `agent/middleware.py`, `agent/tools/*`, `observability/trace.py` | `GuardMiddleware.wrap_tool_call` enforces tool whitelist, `extra=forbid` args schemas, parsed target-metric scope, explicit user-scope preservation starting at `detect_anomaly`, run-scoped deterministic budgets, execution, trace persistence with `token_usage`, and evidence-id invariant for data tools. A tool metric_id that differs from parsed run target returns recoverable `METRIC_SCOPE_VIOLATION` before handler execution and before budget increments. Short-circuits return typed ToolMessages/observations and do not call handlers. A first Pydantic/tool-name `ACTION_SCHEMA_INVALID` is recoverable; the second consecutive illegal schema/tool call to the same tool marks the run failed. Explicit-scope `ACTION_SCHEMA_INVALID` is a recoverable planning precondition and does not participate in the schema-invalid fail streak. Typed tool precondition/planning errors such as `EVIDENCE_MISSING` and pre-E4 duplicate signal fetch `E3_ALREADY_EXISTS` are recoverable and do not consume run budget; a data tool that returns `ok=true` without evidence remains a terminal implementation defect. Step budget blocks further data-fetching tools but does not block `rank_root_causes` or `write_todos`. | `test_middleware_rejects_unknown_tool_without_handler`, `test_metric_scope_violation_short_circuits_without_budget_or_handler`, `test_existing_e3_blocks_additional_signal_fetch_before_e4_without_budget`, `test_explicit_scope_filter_prevents_switching_dimension`, `test_repeated_explicit_scope_errors_do_not_trip_schema_invalid_fail_streak`, `test_explicit_question_scope_requires_detect_filter_before_execution`, `test_evidence_ids_with_wrong_run_prefix_are_recoverable_precondition_errors`, `test_single_illegal_args_then_valid_call_continues`, `test_two_consecutive_illegal_args_same_tool_fails_run`, `test_recoverable_tool_precondition_error_does_not_fail_run`, `test_step_budget_allows_finalizer_and_rejects_data_tool`, `test_data_tool_missing_evidence_id_is_typed_error`, `test_trace_step_persists_token_usage`. | P6/P7 | **P0:** prompt-only guard; LLM-visible counters; handler executes after schema failure; first tool call silently changes run metric; metric-scope rejection burns budget; missing evidence accepted; finalizer blocked by exhausted step budget; recoverable tool argument/precondition error terminates or burns budget before the LLM can correct it; downstream tools silently switch away from explicit user scope; evidence id typos reach deterministic tools as ambiguous missing evidence. |
| 15 | TraceStep / Observability v2 | §16; §9; final-design/01 §8; final-design/02 §6 | `data/schema.sql`, `repositories/metric_repository.py`, `observability/trace.py`, `agent/middleware.py` | Every middleware-managed tool step writes contiguous `trace_step` with latency, error code, and nullable `token_usage` JSON. SQL audit remains tied to guarded plans. Failed runs carry error_code on trace_step and agent_run. | `test_trace_step_schema_has_token_usage_json`, `test_repository_round_trips_trace_step_token_usage`, `test_middleware_persists_trace_for_success_and_error`, existing trace/audit tests. | P6 | stdout logging; missing token accounting column; `except: continue`; trace only outside tool calls. |
| 16 | Evidence E1–E4 binding | §6 (463–478); §15; roadmap §3.4; ADL-0009/0010 | tools/*, `agent/runner.py`, `agent/reflection.py`, `reporting/projector.py`, repos, domain Evidence | Each data tool emits Evidence(...guard_status=passed...) persisted w/ run_id; candidates ref evidence_ids; report numbers are projected from persisted artifacts; current-run evidence required. Repeating the same evidence slot is idempotent only when the existing guard-passed row belongs to the same run and its persisted summary matches the requested metric/dimension/element/input evidence ids; otherwise normal guarded execution or typed failure applies. Additional discovery drilldowns/signals may use E2/E3-family aliases such as `E2_category`, `E3_ch_paid_ads`, and `E3_cat_electronics`; alias generation must keep full evidence_id within the 64-character schema limit. Reflection/eval/report still require an E1/E2-family/E3-family/E4 evidence chain. E_rank requires persisted E4 `sql_text`; missing provenance fails typed instead of fabricating SQL. | gmv → E1–E4 bound to candidate; every candidate.evidence_ids non-empty → guard-passed evidence; every report number found in some result_summary; candidate w/o current-run evidence rejected by reflection; `test_fetch_related_signal_covers_campaign_inventory_conversion_refund_quality`; `test_eval_run_ids_leave_room_for_long_evidence_aliases`; `test_reflection_accepts_related_signal_metric_for_aliased_e3_evidence`; `test_drilldown_repeated_same_evidence_slot_returns_persisted_result_without_requery`; `test_rank_root_causes_requires_persisted_e4_sql_text`; `test_rank_root_causes_invokes_internal_adtributor_and_updates_e4`. | P2→P7 | **P0:** root cause w/o current-run Evidence; null evidence_ids; untraceable numbers; memory-derived conclusion; swallowing duplicate writes without checking persisted context; placeholder rank provenance; blocking multi-dim discovery by overwriting E2/E3 alias families; relying on DB write failure to detect overlong evidence ids. |
| 17 | Anomaly detection | §12 (871–879); roadmap §4.3 | `services/anomaly_service.py`, `services/metric_service.py`, `tools/detect_anomaly.py` | Baseline = prev 4 same weekday; mean/std; delta_pct; z=delta/max(std,eps); is_anomaly=`|delta_pct|≥0.15 and |z|≥2.0`; sample_n<3→INSUFFICIENT_BASELINE_DATA; below→NO_ANOMALY_DETECTED; emits Baseline+Evidence. | Known seeded values → expected mean/std/delta/z; paid_ads flagged; gmv_no_anomaly → NO_ANOMALY_DETECTED (explicit success, not coverage-fail); sample_n<3 → INSUFFICIENT_BASELINE_DATA; threshold boundaries. | P2 | Wrong baseline (yesterday/all-day mean); no-anomaly as failure; hardcoded is_anomaly. |
| 18 | Attribution + GMV=UV·PAY_CVR·AOV | §12 (881–893); roadmap §4.4 | `services/attribution_service.py`, `services/metric_service.py`, `tools/calculate_contribution.py` | `drop_by_dim=max(0,baseline-current)`, `contribution_pct=drop/Σdrop`; `GMV=UV·PAY_CVR·AOV`, `PAY_CVR=pay_user_cnt/uv`, `AOV=gmv/pay_user_cnt` (**not** gmv/pay_orders); `net_gmv=gmv-refund`; `score=contribution·severity·evidence_support·reflection_factor`, `eng_confidence` engineering-only. GMV factor decomposition needs fact_traffic and is only emitted for `metric_id="gmv"`; `net_gmv` emits guarded gmv/refund/net_gmv factor evidence; pay_cvr/refund_rate must not fabricate GMV-only decomposition. | paid_ads contribution ≥0.8 → top1; UV·PAY_CVR·AOV reconstructs GMV within tolerance; largest relative-drop factor (UV) flagged; calculate_contribution net_gmv executes guarded gmv + net_gmv factor queries and emits gmv/refund/net_gmv split; pay_cvr/refund_rate calculate_contribution results have no GMV decomposition; assert no `pay_orders` reference. | P2 | **P0:** empty-data attribution; gmv/pay_orders; missing net_gmv gmv/refund split; GMV decomposition emitted for unrelated metrics; eng_confidence as statistical. |
| 19 | Reflection verifier & orchestrator repair | §7; MetricRCA §5 v2; final-design/01 §5; ADL-0006/0008 | `agent/reflection.py`, `agent/runner.py`, `reporting/projector.py` | Reflection remains an eight-check deterministic rule verifier but now reads persisted artifacts only. Error issues may trigger one orchestrator-owned repair re-entry through the same deepagents thread; still failing means `REFLECTION_REPAIR_FAILED`, run failed, and no report projection. | `test_reflection_without_persisted_evidence_map_fails_in_v2_mode`, `test_reflection_repair_exhausted_no_report`, `test_repair_guard_failure_stops_before_report`, existing ADL-0006 reporting tests. | P6 | **P0:** Reflection stub/summary; report after failed Reflection; repair increments counter without new guarded evidence. |
| 20 | Memory required boundary under orchestrator | §8; final-design/02 §5; final-design/03 §1 | `memory/memory_repo.py`, `agent/runner.py` | Memory repository remains real DB-backed storage. In P6 the orchestrator/finalizer treats required read/write failures as typed run failures. Memory hits may influence prompts/planning priority only and cannot become final evidence or candidates. | `test_memory_required_read_failure_fails_run`, `test_memory_write_failure_fails_run_when_required`, `test_memory_cannot_be_final_conclusion_without_current_evidence`, existing memory TTL/confidence/version tests. | P6 | **P0:** swallowing memory failure; memory-derived conclusion; memory evidence_id accepted. |
| 21 | FastAPI API | §14 (935–982); final-design/01 §1 | `api/main.py`, `api/routes.py`, `agent/runner.py` | Real FastAPI: `POST /api/rca/runs` synchronously invokes `RunOrchestrator`/`run_rca` and returns run_id+status after persisted side effects; `GET /api/rca/runs/{run_id}`(+report+candidates), `/trace`, `/evidence`, `POST /api/evals/run`, `GET /api/evals/{eval_id}`, `GET /health`; 422 on bad body; unified error body (error_code/message/recoverable/retryable/trace_step_id/suggested_next_action). | TestClient + **spy on `run_rca()`/repos**: POST invokes runner & persists agent_run; GET returns *persisted* artifacts (route-hardcoded data fails); /health ok; bad body → 422; guard-reject path → structured error body; eval endpoints return summary. | P4/P6 | **P0:** CLI print posing as FastAPI; missing endpoints; route-level hardcoded data; stub not invoking runner. |
| 22 | React/Vite debug UI | §19 (1069–1081); roadmap §8.2 | `frontend/` | Real React/Vite panels: question input+run, conclusion, root-cause Top-K from persisted E4 candidates, evidence table (sql/hash/guard/result), sql_audit, ReAct trace timeline, reflection issues, memory hits/status, eval summary; reads API only. | **Inject fake API client**: assert UI calls documented endpoints and renders returned trace/evidence/reflection; module imports w/o firing network or RCA run; 9 panels present. | P4 | **P0:** print(json) posing as UI; missing panels; UI fabricating data. |
| 23 | Eval runner / scorer | §17 (1023–1044); contract (181–185) | `evals/cases.jsonl`, `evals/runner.py`, `evals/scorer.py` | Read GT+EvalCase; run RCA/case; per-case intent_ok/anomaly_ok/top1_ok/top3_ok/evidence_coverage/sql_safe/reflection_repair_ok → eval_case_result; summary case_total/dangerous_sql_blocked/no_anomaly_correct+hit-rates → eval_run.summary; `dangerous_sql_blocked` real bool; `no_anomaly_correct` checks no operation_task; JSON+MD; missing GT → EVAL_GROUND_TRUTH_MISSING. | 5 cases: 4 top1 hits + no_anomaly correct; rows written; **mutate one GT row → scorer output changes**; **delete GT row → EVAL_GROUND_TRUTH_MISSING**; **dangerous_sql_blocked from real guard rejection (never constant)**; no_anomaly_correct=False if task created; sql_safe=100%. | P5 | **P0:** hardcoded eval success; `dangerous_sql_blocked=null`; echoing answers. |
| 24 | Zero-Fallback negative tests v2 | §11, §18; final-design/01 §3/§5/§6; prompt 07 | `tests/test_zero_fallback.py` (+ orchestrator/middleware/tool behavior) | Automated negatives cover the deepagents migration: required LLM failure, illegal args, budget exhaustion, SQLGuard non-bypass, no-anomaly contract, Reflection repair exhaustion, required memory failure, and empty result insufficient-evidence path. No production mocks, broad exception swallowing, or deterministic policy fallback. | `test_llm_unavailable_fails_before_agent_loop`, `test_illegal_tool_args_observation_tool_not_executed`, `test_budget_exhausted_then_llm_attempts_data_tool_run_failed`, `test_step_budget_allows_finalizer_and_rejects_data_tool`, `test_sql_guard_rejection_cannot_be_bypassed`, `test_no_anomaly_with_drilldown_or_rank_trace_fails`, `test_reflection_repair_exhausted_no_report`, `test_memory_read_write_failure_fails_run`, `test_empty_result_set_no_attribute_or_rank`. | P6 | Manual checklist; tests that mock away the core path; passing by deterministic production policy; exception swallowing; finalizer blocked by budget instead of typed data-tool rejection. |
| 25 | README & architecture docs | roadmap 阶段5; §10; contract §5 | `README.md`, `docs/architecture.md`, `screenshots/` | README: architecture, Zero-Fallback boundary, sole data path `QuerySpec→SQLRenderer→SQLGuard→Repository`, make targets, error codes, target response; architecture.md mermaid; screenshots of UI. | **Cross-check** documented commands/endpoints/error codes against actual Makefile targets/route defs/error enums; architecture.md has mermaid; require fresh `make seed && make eval && make test` evidence. | P5 | README claims features not built; omitting Zero-Fallback; reframing MVP as "small prototype" (contract §2). |
| 26 | Config / settings | §4 (348); §5 (416); §12; ADL-0011; roadmap 阶段1 | `config/settings.py` (pydantic-settings), `services/llm_client.py` | Typed settings: MAX_STEPS/MAX_QUERY/MAX_DRILLDOWN_DEPTH/MAX_REPAIR, THRESH_PCT/Z_THRESH, DSN, tz, business dates, llm/memory enabled+required, `llm_base_url`, `llm_structured_output_method`; no default provider/config substitution. OpenAI-compatible providers must switch by configuration only and require explicit base_url/key; third-party providers must not silently reuse `OPENAI_API_KEY`. | Defaults == documented values; required-but-missing setting raises (no silent default); llm_required + no provider → LLM_REQUIRED_UNAVAILABLE at run; openai-compatible without base_url → typed `LLM_BASE_URL_REQUIRED`; `OPENAI_API_KEY` is not substituted for openai-compatible providers; DeepSeek smoke proves provider/model/base_url/method are config-driven. | P1/P7 | Magic numbers scattered; silent default provider/config; limits only in graph code; hardcoded provider branches in business logic; catching provider failure and auto-switching structured-output method/model. |
| 27 | Extended Tool Contracts | §13 (910–931) | `agent/tools/*` / `services/*` | Pydantic In/Out schemas + gated behavior for `get_metric_definition`, `get_schema_context`, `rank_root_causes`, `verify_evidence`, `search_memory`, `create_operation_task`. Metadata contracts are DB/schema-backed; fixed question families do not authorize hardcoded metric definitions, seeded dimension values, or schema context. Runtime config mappings must be complete, overrideable, and tested as explicit simplifications, not immutable business facts. | Each returns its typed Out or documented error (METRIC_NOT_FOUND, SCHEMA_CONTEXT_MISSING, ATTRIBUTION_COVERAGE_LOW, EVIDENCE_MISSING, MEMORY_READ_FAILED); In rejects extra fields; metadata mutation/deletion changes runtime output/error; renderer/domain/seeded metadata consistency tests pass; config override test proves root-cause/signal mapping is not hardcoded; incomplete or non-whitelist signal metric config raises CONFIG_INVALID; source scan rejects duplicated metric-definition/schema-context constants. | P2–P3 | Untyped helper dicts; missing error codes; hardcoded metric/schema dictionaries; config defaults treated as unoverrideable facts; raw KeyError for config gaps; verify_evidence not enforcing current-run evidence. |
| 28 | RootCauseCandidate v2 + EP ranking | final-design/02 §2; MetricRCA §12 P7 | `domain/models.py`, `services/attribution_service.py`, `services/adtributor_service.py` | Candidate keeps v1 single-dim fields and adds `dimension_elements`, `explanatory_power`, `surprise_js`. When Adtributor has produced EP, ranking uses EP as contribution score and JS surprise as tie-breaker; non-Adtributor candidates keep the v1 formula. | `test_root_cause_candidate_v2_serialization_round_trips`; `test_rank_root_causes_uses_ep_before_v1_formula_for_adtributor_candidates`; existing attribution ranking tests still pass for v1. | P7 | Breaking v1 candidate JSON; sorting by eng_confidence only; storing multi-dim findings as free text instead of typed fields. |
| 29 | Adtributor deterministic service | final-design/02 §3; MetricRCA §12 P7 | `services/adtributor_service.py` | Pure deterministic EP/JS implementation. Additive metrics compute `EP=(A_i-F_i)/(A-F)` with forecast from Evidence; JS uses finite Jensen-Shannon formula. Greedy selection orders by surprise, requires `EP>T_EEP`, stops at `T_EP`, and returns typed `ADTRIBUTOR_NOT_APPLICABLE` for unsupported ratio input. No DB/repository/fact/ground-truth imports. | `test_adtributor_additive_ep_sums_to_one_for_dimension`; `test_adtributor_js_is_symmetric_and_bounded`; `test_adtributor_greedy_selection_thresholds_change_selected_elements`; service purity scan asserts no repository/SQL imports. | P7 | SQL inside Adtributor; hardcoded dimension values; clamping JS to hide zero cases; silently returning wrong ratio EP. |
| 30 | Ranker-internal Adtributor | final-design/02 §3/§8; MetricRCA §12/§13; ADL-0009 | `agent/deep_tools.py`, `services/adtributor_service.py`, `agent/tools/drilldown_dimension.py`, `repositories/metric_repository.py` | `adtributor_attribute.py` must not exist and must not be exposed to the LLM. `rank_root_causes` reads only current-run persisted E4 plus drilldown Evidence summaries containing per-element actual/forecast, invokes pure `adtributor_service` when applicable, writes EP/surprise and typed `dimension_elements` into E4 selected/candidates, and persists E_rank. It never calls `execute_plan`, never accepts literal element values from the prompt, and non-applicable Adtributor leaves the single-dim path explicit rather than fabricating EP. C06 must prove multi-element; C07 must prove both `(channel, paid_ads)` and `(category, electronics)`. | `test_rank_root_causes_invokes_internal_adtributor_and_updates_e4`; `test_eval_requires_dimension_elements_for_c06_c07`; `test_eval_c07_requires_exact_channel_category_pair`; compiled deepagents tool list excludes any standalone Adtributor tool; source scan finds no `metric_rca/agent/tools/adtributor_attribute.py`. | P7 | Reintroducing Adtributor as an LLM tool; reading fact tables/SQL/ground_truth inside Adtributor; receiving literal per-element values from prompts; untyped dict inputs; silently skipping missing Evidence; collapsing C07 to a single dimension. |
| 31 | net-GMV chain decomposition | final-design/02 §4; MetricRCA §12 P7 | `services/attribution_service.py`, `agent/tools/calculate_contribution.py` | `net_gmv_chain` first splits GMV delta vs refund delta contribution, then continues the dominant side: GMV side emits UV/PAY_CVR/AOV decomposition, refund side emits refund contribution context. It preserves QuerySpec→SQLGuard→Repository and never introduces `fact_traffic.pay_orders`. | `test_net_gmv_chain_identifies_refund_driven_case`; `test_net_gmv_chain_identifies_gmv_driven_case`; source scan rejects `pay_orders`. | P7 | Only reporting net_gmv current/baseline; adding unauthorized DDL; using GMV-only decomposition for refund-driven cases. |
| 32 | 20-case seeded anomaly library | final-design/02 §9; MetricRCA §10 P7 | `data/anomaly_injection.py`, `data/seed_data.py`, `evals/cases.jsonl` | Seed deterministically writes C01-C20 ground truth rows, preserving MVP case ids for C01-C05 and adding C06-C20. C19/C20 use `2026-06-04`, expected no anomaly, and all rows include metric/dimension/element or explicit no-anomaly nulls. Seed remains idempotent. | `test_seed_metric_definitions_and_ground_truth_cases` asserts all 20 rows; `test_seed_is_idempotent_and_has_required_calendar`; trap case test asserts C19/C20 no-anomaly dates/labels. | P7 | Editing ground truth to match current output; omitting dimension/element; placing no-anomaly traps on injected target date; non-idempotent seed. |
| 33 | 20-case eval scoring + integrity | final-design/02 §9/§9.1/§9.2; MetricRCA §17; ADL-0009 | `evals/runner.py`, `evals/scorer.py`, `evals/cases.jsonl` | `make eval` runs all 20 persisted-artifact cases and records `adtributor_used`, `multi_agent_path`, `llm_provider`, and `llm_model`. Summary thresholds: case_total=20, intent/anomaly/sql/report=1.0, top1>=0.80, top3>=0.90, no-anomaly traps true. Eval questions are natural-language business questions: no `metric_id=` literal, no root-cause mechanism answer leakage, and discovery cases C06/C07/C08/C09 do not name the dimension/element to be found. C06 requires exact paid_ads+social `dimension_elements`; C07 requires exact paid_ads+electronics cross. `gpt-4.1-mini` is rejected for P7 acceptance. C19/C20 require no candidate/task/downstream trace. | `test_eval_summary_accepts_p7_thresholds`; `test_eval_scores_adtributor_used_and_multi_agent_path`; `test_eval_requires_dimension_elements_for_c06_c07`; `test_eval_c07_requires_exact_channel_category_pair`; `test_eval_rejects_gpt_4_1_mini_for_p7_acceptance`; `test_no_anomaly_correct_requires_all_no_anomaly_traps_clean`; natural-question source scan; 2x live `make eval` evidence with capable model. | P7 | Hardcoded eval success; fake LLM eval; answer-leaking questions; using `gpt-4.1-mini` for acceptance; only checking the original five cases; allowing false-positive candidates on C19/C20; relaxing C07 to a single dimension. |

**Note:** Rows 1–25 cover the 25 mandated requirement areas; rows 26–33 are additional foundation/contract gates (config, extended tool contracts, P7 Adtributor/net-GMV/20-case expansion) the docs require but that fall outside the mandated list.

---

## Build sequence
- **P1:** rows 1–10, 26 — deps, Makefile, compose, schema, seed, domain, QuerySpec, renderer, guard, repo+audit, config. Gate: `make up && make seed && make test` with test_guard/test_schema/test_seed green; seed idempotent.
- **P2:** rows 12, 16(partial), 17, 18, 27 — services + tools + evidence emission. Gate: test_anomaly/test_attribution/test_tools green without LLM.
- **P6:** rows 11, 13, 14, 19, 20, 24 — deepagents factory, GuardMiddleware, RunOrchestrator, persisted-artifact Reflection, memory boundary, zero-fallback v2. Gate: test_orchestrator/test_middleware + zero-fallback v2 + full suite green; live eval additionally requires configured LLM credentials.
- **P4:** rows 21, 22 — API + UI. Gate: test_api green; UI structural test green.
- **P5:** rows 23, 25, finalize 24 — eval, README/architecture, screenshots. Gate: `make seed && make eval && make test` + unittest discover; 5 cases green incl. no_anomaly no-task; sql_safe=100%.
- **P7:** rows 28–33 — Adtributor service, ranker-internal Adtributor, RootCauseCandidate v2, net-GMV chain, natural-question 20-case seed/eval. Gate: full pytest count grows beyond P6; `make seed && make eval` runs 20 cases twice with a capable model (not `gpt-4.1-mini`) and thresholds met, including C06/C07 dimension_elements and C19/C20 clean.

## Provenance — Codex (5.5 / high) strict review
The original matrix was reviewed adversarially by Codex and returned **FAIL**; no direct concept-substitution, but several proof-tests were too weak to fail against a shortcut. All blocking findings are folded into the rows above:
- Row 19 repair test now enforces full re-entry into the legal ReAct→QuerySpec→SQLRenderer→SQLGuard→Repository path.
- Row 23 eval now requires GT mutation/deletion proof and a real guard-rejected `dangerous_sql_blocked`.
- `metric_rca/services/metric_service.py` added to rows 12/17/18.
- Row 14 gates `METRIC_NOT_FOUND` vs `PARSE_FAILED` separately; rejects re-export shells.
- Row 2 Makefile `api` includes `--reload`; Row 21 endpoint label `{eval_id}`.
- Rows 1/21/22/25 strengthened (clean-venv install, run_rca/repo spies, fake-API-client, doc cross-check).
- Row 20 adds version-conflict resolution test; Row 27 added for extended Tool Contracts.

## Open decisions (do not silently default)
1. Live eval requires configured deepagents/LLM credentials; without them, P6 must fail typed `LLM_REQUIRED_UNAVAILABLE` rather than falling back to deterministic action selection.
2. Eval cases as `cases.jsonl` (roadmap 阶段5) vs `cases.py` (module layout) — jsonl recommended.
3. `scikit-learn` is deferred beyond P1; MVP anomaly math uses deterministic mean/std only.
