# MetricRCA — Strict Docs Compliance Matrix (Binding Implementation Gate)

**Base commit** `b6b4b5b` (docs-only). **Current status:** P1 rows 1–10 and 26 are implemented in this worktree; P2+ rows remain not implemented. The later "shortcut" implementation that exists in history MUST NOT be cherry-picked — it is cited only as an anti-pattern.

**Authority order** (per `docs/IMPLEMENTATION_CONTRACT.md` §1): current user instruction → `AGENTS.md`/contract → `docs/MetricRCA.md` → `docs/MetricRCA-roadmap-checklist.md` → `docs/deep-research-report.md` (background only).

**This file is the binding gate.** A change is acceptable only if it satisfies the relevant row's *Required behavior* AND its *Tests that must prove it* pass — where each proof test is written so the forbidden shortcut turns it red. Keep the "Current status" understanding updated as phases land; do not weaken a test to fit a shortcut.

## Phase legend (roadmap §9)
P1 Data+Guardrail · P2 Services+Tools · P3 LangGraph · P4 API+UI · P5 Eval+Docs.
Foundation rows (domain models, config) are built at the start of P1 because everything depends on them.

## Process rule (contract §5 / AGENTS.md)
For each row: write the proof test(s) FIRST (encoding the docs), confirm they fail against a shortcut, then implement until green. Final completion runs:
`make seed && make eval && make test && python -W error::ResourceWarning -m unittest discover -s tests -v`.

---

## Compliance Matrix

| # | Requirement | Docs ref | Required files | Required behavior | Tests that must prove it | Phase | Shortcut risk to avoid |
|---|---|---|---|---|---|---|---|
| 1 | Dependencies & pyproject | §20 (1086–1097); roadmap 阶段1; current P1 scope | `pyproject.toml` | Package `metric_rca`, Py3.12; declare only P1 MVP deps with pinned constraints: pydantic>=2, pydantic-settings, sqlalchemy>=2, pymysql, sqlglot, pandas, pytest. FastAPI/uvicorn/Streamlit/httpx/LangGraph/LangChain/scikit-learn are Phase >1 and must not be declared now. | Clean venv `pip install -e .`, then parse installed metadata: assert exact P1 deps **declared with pinned constraints**; assert Phase >1 deps absent; `pydantic.version_info.major==2`; sqlalchemy>=2. | P1 | Lighter-lib substitution; pydantic v1; deps importable from ambient env but undeclared; pulling Phase >1 deps into P1. |
| 2 | Makefile | §20 (1099–1107); current P1 scope | `Makefile` | `up`=`docker compose up -d mysql`; `seed`=`python -m metric_rca.data.seed_data`; `test`=`pytest -q`; `eval` may exist but must only call the Phase >1 placeholder and must not echo success. No API/UI targets in P1. | Parse Makefile, assert each P1 target == documented command; assert no uvicorn/streamlit target; `make test` runs pytest. | P1 | `echo`/`true` targets; eval printing fake success; API/UI targets pretending P1 completion. |
| 3 | Docker / local DB | §20 (1092,1109); §11 (839–842); current P1 scope | `docker-compose.yml` | Service mysql(8.x, init schema.sql, healthcheck); execution via read-only DB account; statement timeout. No API/UI services in P1. | Parse compose: only mysql service; mysql 8.x; schema.sql init mount; integration connect to seeded DB; DML via exec conn raises. | P1 | SQLite substitution; skipping read-only account; sneaking API/UI services into P1. |
| 4 | schema.sql | §9 (530–727); roadmap §1.2/1.3 | `metric_rca/data/schema.sql` | All 17 tables w/ documented cols/types/keys incl. **fact_customer_ticket**, memory_record, eval_run, eval_case_result; `business_date DATE`, system ts `DATETIME`. | After seed, introspect `information_schema`: all 17 tables w/ documented columns/types; no `fact_ticket`; PKs/indexes present. | P1 | Dropping "optional" system tables; `fact_ticket`; TIMESTAMP/VARCHAR dates. |
| 5 | seed_data deterministic 60-day + 5 GT cases | §10 (745–763); roadmap §4 | `data/seed_data.py`, `data/anomaly_injection.py` | SEED=20260606, business_today=2026-06-06, target=2026-06-05, 60 days, Asia/Tokyo; weekday/channel/category/campaign/inventory/refund effects; seed metric_definition; inject 5 cases + write anomaly_ground_truth; idempotent. | Seed twice → identical content hash; 60 distinct business_date; anomaly_ground_truth = exactly 5 documented case_ids w/ correct fields; `gmv_no_anomaly.expected_anomaly=0`; paid_ads spend/clicks/uv below same-weekday baseline. | P1 | Unseeded RNG; GT answers hardcoded in agent; non-idempotent seed. |
| 6 | Pydantic / domain models | §2 (70–268); roadmap §3.9 | `domain/enums.py`, `domain/models.py` | All enums + models (TimeRange…AgentRun); every contract model `ConfigDict(extra="forbid")`; v2 validators. | Each model rejects unknown field (ValidationError); enum membership; `model_dump`/`model_validate` round-trip. | P1 | dataclasses/dicts; missing `extra="forbid"`; pydantic v1. |
| 7 | QuerySpec | §2 (145–159); §11; roadmap §3.2 | `guardrails/query_spec.py` (+domain) | metric_id, time_range, group_by(≤2 validator), filters, limit le=5000, purpose enum; `build_query_spec` whitelist-only; never free SQL. | group_by>2 raises; limit>5000 raises; bad purpose raises; non-whitelist dim → DIMENSION_NOT_ALLOWED/QUERY_SPEC_INVALID. | P1 | Raw SQL in QuerySpec; unrestricted group_by. |
| 8 | SQLRenderer | §11 (773–775); roadmap §2.3 | `guardrails/renderer.py` | Deterministic template per (metric, group_by) → parameterized SQL + sha256 hash → SQLPlan; always injects fact `business_date` + LIMIT; no CTE/subquery/`*`; category → whitelist INNER JOIN to dim_product. | gmv current → single SELECT w/ date+LIMIT, no star; category → exact whitelist JOIN; channel → no join; **all 6 question-family renders pass SQLGuard**; hash==sha256. | P1 | Concat allowing column injection; LLM joins; output that its own guard rejects. |
| 9 | sqlglot AST SQLGuard | §11 (777–865); roadmap §5 | `guardrails/sql_guard.py` | AST via `sqlglot.parse`: reject multi-stmt, non-SELECT, DML/DDL/Command, `*`, CTE, subquery/derived, non-whitelist table/col, missing fact table, missing fact business_date, missing LIMIT, non-renderer joins; allow only whitelist INNER JOIN; preserve hash; feed sql_audit. | 9 documented cases + column-whitelist/non-renderer-JOIN/missing-LIMIT rejects; **AST-proof**: comment/case-obfuscated DROP & derived table still rejected (defeats naive regex). | P1 | **P0:** regex guard posing as AST; keyword-only checks; arbitrary joins. |
| 10 | Repository & SQL audit | §4 (343); §11 (839–844); §9 | `repositories/*.py` | Execute ONLY passed SQLPlan via `text()` parameterized, read-only acct, timeout, LIMIT; every SQL → sql_audit row (hash/guard_status/errors/row_count/latency); persist all system tables; never run raw SQL. | Raw/un-guarded SQL raises (`test_sql_guard_rejection_cannot_bypass_renderer`); each fact query writes sql_audit w/ correct hash+row_count; filter values param-bound (injection stays literal); DML raises. | P1→P2 | Direct SQL exec w/o guard; skipping sql_audit; f-string params. |
| 11 | AgentAction & ReAct policy | §6 (443–461); contract (99–111) | `agent/react.py` (+domain) | ALLOWED_ACTIONS=[detect_anomaly,drilldown_dimension,fetch_related_signal,calculate_contribution,finish]; deterministic-primary policy (optional LLM whitelist-bound); illegal/bad-args → `Observation(ok=False, ACTION_SCHEMA_INVALID)`, no tool; LLM required+down → LLM_REQUIRED_UNAVAILABLE; `finish` is control. | illegal action → ACTION_SCHEMA_INVALID + no tool; bad args → same; policy yields documented gmv sequence; `test_llm_required_unavailable_fails`, `test_illegal_action_records_error_and_does_not_execute_tool`. | P3 | **P0:** LLM free tool-calling; silent rewrite+continue; LLM writes SQL. |
| 12 | Tool layer | §13 (910–931); contract (114–133) | `agent/tools/{detect_anomaly,drilldown_dimension,fetch_related_signal,calculate_contribution}.py` (+ `services/metric_service.py`) | Each: typed In/Out, QuerySpec→Renderer→Guard→Repo, return Observation+Evidence, write sql_audit, never bypass guard; typed errors. | Each returns Observation; data tools bind guard-passed Evidence; assert SQLPlan.guard_status=passed pre-execute; non-whitelist dim → DIMENSION_NOT_ALLOWED; sample_n<3 → INSUFFICIENT_BASELINE_DATA; empty → no attribution. | P2 | **P0:** empty placeholder tools; direct DB read; fabricated payload w/o Evidence. |
| 13 | LangGraph StateGraph | §5 (361–437); contract (74–82) | `agent/graph.py`, `agent/state.py` | Real `StateGraph(RCAState)` w/ START/END, add_node/edge/conditional_edges; TypedDict w/ `Annotated[list, add]` reducers; documented conditional routing; termination via max_steps=8/max_query=12/max_drilldown_depth=2/max_repair=1 (NOT recursion_limit); `run_rca()` invokes compiled graph. | Introspect compiled graph: node set + conditional mappings == docs; reducers accumulate; tiny max_steps → stops by business limit not GraphRecursionError; E2E gmv → campaign_traffic_drop. | P3 | **P0:** sequential fn posing as graph; recursion_limit as safety; lost reducers. |
| 14 | Agent node files | §5 (398–410); contract (84–97) | `agent/nodes/{parse_question,read_memory,plan_init,react_step,execute_tool,attribute_rank,reflection_verify,generate_report,create_tasks,write_memory,error_return}.py` | Each real module w/ documented read/write-state + failure edge. | Per-node state-mutation + failure tests; **`METRIC_NOT_FOUND` and `PARSE_FAILED` gated separately**; execute_tool writes trace_step+evidence; attribute_rank low coverage → ATTRIBUTION_COVERAGE_LOW; **reject re-export shells via `__module__`/source path**. | P3 | **P0:** empty/re-export node modules; collapsing nodes; skipping trace. |
| 15 | TraceStep / Observability | §16 (1009–1019); §9 | `observability/trace.py` (+repo) | Every step writes TraceStep (seq/node/action/in/out/error_code/latency); every SQL → sql_audit; failures carry error_code on trace_step + agent_run; replay via fixed seed+QuerySpec. | After run: contiguous seq per run_id over every visited node; each evidence step has sql_audit row; failed run has error_code on failing node + agent_run; latency ≥0. | P3 (table P1) | stdout logging; missing steps; no error_code; `except: continue`. |
| 16 | Evidence E1–E4 binding | §6 (463–478); §15; roadmap §3.4 | tools/*, nodes/attribute_rank+generate_report, repos, domain Evidence | Each data tool emits Evidence(...guard_status=passed...) → state.evidences, persisted w/ run_id; candidates ref evidence_ids; report numbers traceable; current-run evidence required. | gmv → E1–E4 bound to candidate; every candidate.evidence_ids non-empty → guard-passed evidence; every report number found in some result_summary; candidate w/o current-run evidence rejected by reflection. | P2→P3 | **P0:** root cause w/o current-run Evidence; null evidence_ids; untraceable numbers; memory-derived conclusion. |
| 17 | Anomaly detection | §12 (871–879); roadmap §4.3 | `services/anomaly_service.py`, `services/metric_service.py`, `tools/detect_anomaly.py` | Baseline = prev 4 same weekday; mean/std; delta_pct; z=delta/max(std,eps); is_anomaly=`|delta_pct|≥0.15 and |z|≥2.0`; sample_n<3→INSUFFICIENT_BASELINE_DATA; below→NO_ANOMALY_DETECTED; emits Baseline+Evidence. | Known seeded values → expected mean/std/delta/z; paid_ads flagged; gmv_no_anomaly → NO_ANOMALY_DETECTED (explicit success, not coverage-fail); sample_n<3 → INSUFFICIENT_BASELINE_DATA; threshold boundaries. | P2 | Wrong baseline (yesterday/all-day mean); no-anomaly as failure; hardcoded is_anomaly. |
| 18 | Attribution + GMV=UV·PAY_CVR·AOV | §12 (881–893); roadmap §4.4 | `services/attribution_service.py`, `services/metric_service.py`, `tools/calculate_contribution.py` | `drop_by_dim=max(0,baseline-current)`, `contribution_pct=drop/Σdrop`; `GMV=UV·PAY_CVR·AOV`, `PAY_CVR=pay_user_cnt/uv`, `AOV=gmv/pay_user_cnt` (**not** gmv/pay_orders); `net_gmv=gmv-refund`; `score=contribution·severity·evidence_support·reflection_factor`, `eng_confidence` engineering-only; needs fact_traffic. | paid_ads contribution ≥0.8 → top1; UV·PAY_CVR·AOV reconstructs GMV within tolerance; largest relative-drop factor (UV) flagged; net_gmv splits gmv vs refund; assert no `pay_orders` reference. | P2 | **P0:** empty-data attribution; gmv/pay_orders; eng_confidence as statistical. |
| 19 | Reflection verifier & repair | §7 (482–499); §5; contract (150–154) | `agent/reflection.py`, `nodes/reflection_verify.py` | Rule verifier: evidence_coverage, current-run evidence, guard_status=passed, metric_consistency, time_range, attribution_coverage, no-anomaly→no task, causal-language, insufficient_data, repair limit; error+suggested_action → react_step (legal QuerySpec+guard), repair_count+=1, MAX_REPAIR=1; fail → REFLECTION_REPAIR_FAILED; never report after failed reflection. | Missing evidence → fail; untraceable number → fail; **fixable issue → suggested_action becomes AgentAction → schema pass → guarded SQLPlan → new Evidence+TraceStep → re-verify pass** (`passed` via repair_count++ alone must fail test); unfixable → REFLECTION_REPAIR_FAILED + NO report; repair>MAX → error_return; "导致" w/o confirmed evidence flagged. | P3 | **P0:** reflection stub/2nd summary; report after fail; repair bypassing guard; unconditional pass. |
| 20 | Memory repository | §8 (503–522); §13; contract (156–168) | `memory/memory_repo.py`, `nodes/read_memory.py`, `nodes/write_memory.py` | Real repo (not re-export); read by exact key `gmv\|channel`; hits influence ONLY drilldown priority, never conclusion; respect confidence/source/version/ttl_days; low-conf excluded; expired ttl unread; version conflict→higher; required failure → typed error→error_return; reflection_factor boost ≤1.2 only w/ independent current-run evidence. | Hit reorders drilldown but conclusion still needs current-run evidence; low-conf ignored; expired ttl not returned; **version-conflict: higher version wins, lower can't plan**; `test_memory_required_read_failure_fails_run`, `test_memory_required_write_failure_fails_run`; memory_enabled=false → not called. | P3 | **P0:** memory_repo re-export shell; memory conclusion w/o evidence; ignoring confidence/ttl/version; swallowing failure. |
| 21 | FastAPI API | §14 (935–982); contract (170–179) | `api/main.py`, `api/routes.py` | Real FastAPI: `POST /api/rca/runs` (sync graph, run_id+status), `GET /api/rca/runs/{run_id}`(+report+candidates), `/trace`, `/evidence`, `POST /api/evals/run`, `GET /api/evals/{eval_id}`, `GET /health`; 422 on bad body; unified error body (error_code/message/recoverable/retryable/trace_step_id/suggested_next_action). | TestClient + **spy on `run_rca()`/repos**: POST invokes compiled graph & persists agent_run; GET returns *persisted* artifacts (route-hardcoded data fails); /health ok; bad body → 422; guard-reject path → structured error body; eval endpoints return summary. | P4 | **P0:** CLI print posing as FastAPI; missing endpoints; route-level hardcoded data; stub not invoking graph. |
| 22 | Streamlit debug UI | §19 (1069–1081); roadmap §8.2 | `ui/app.py` | Real Streamlit panels: question input+run, conclusion, root-cause top-K, evidence table (sql/hash/guard/result), sql_audit, ReAct trace timeline, reflection issues, memory hits, eval summary; reads API. | **Inject fake API client**: assert UI calls documented endpoints and renders returned trace/evidence/reflection; module imports w/o firing network; 9 panels present. | P4 | **P0:** print(json) posing as Streamlit; missing panels; UI fabricating data. |
| 23 | Eval runner / scorer | §17 (1023–1044); contract (181–185) | `evals/cases.jsonl`, `evals/runner.py`, `evals/scorer.py` | Read GT+EvalCase; run RCA/case; per-case intent_ok/anomaly_ok/top1_ok/top3_ok/evidence_coverage/sql_safe/reflection_repair_ok → eval_case_result; summary case_total/dangerous_sql_blocked/no_anomaly_correct+hit-rates → eval_run.summary; `dangerous_sql_blocked` real bool; `no_anomaly_correct` checks no operation_task; JSON+MD; missing GT → EVAL_GROUND_TRUTH_MISSING. | 5 cases: 4 top1 hits + no_anomaly correct; rows written; **mutate one GT row → scorer output changes**; **delete GT row → EVAL_GROUND_TRUTH_MISSING**; **dangerous_sql_blocked from real guard rejection (never constant)**; no_anomaly_correct=False if task created; sql_safe=100%. | P5 | **P0:** hardcoded eval success; `dangerous_sql_blocked=null`; echoing answers. |
| 24 | Zero-Fallback negative tests | §11 (857–865), §18; roadmap §12 | `tests/test_zero_fallback.py` (+ behaviors) | Automated negatives, not manual checklist; required-dependency failure → typed error; no broad `except: continue`. | The 8 named tests: `test_llm_required_unavailable_fails`, `test_illegal_action_records_error_and_does_not_execute_tool`, `test_memory_required_read_failure_fails_run`, `test_memory_required_write_failure_fails_run`, `test_empty_result_does_not_enter_attribute_rank`, `test_sql_execution_retry_exhausted_fails_run`, `test_sql_guard_rejection_cannot_bypass_renderer`, `test_no_anomaly_skips_create_tasks`. | P3→P5 | Manual checklist; tests asserting a shortcut's behavior; exception swallowing making negatives trivially pass. |
| 25 | README & architecture docs | roadmap 阶段5; §10; contract §5 | `README.md`, `docs/architecture.md`, `screenshots/` | README: architecture, Zero-Fallback boundary, sole data path `QuerySpec→SQLRenderer→SQLGuard→Repository`, make targets, error codes, target response; architecture.md mermaid; screenshots of UI. | **Cross-check** documented commands/endpoints/error codes against actual Makefile targets/route defs/error enums; architecture.md has mermaid; require fresh `make seed && make eval && make test` evidence. | P5 | README claims features not built; omitting Zero-Fallback; reframing MVP as "small prototype" (contract §2). |
| 26 | Config / settings | §4 (348); §5 (416); §12; roadmap 阶段1 | `config/settings.py` (pydantic-settings) | Typed settings: MAX_STEPS/MAX_QUERY/MAX_DRILLDOWN_DEPTH/MAX_REPAIR, THRESH_PCT/Z_THRESH, DSN, tz, business dates, llm/memory enabled+required; no default provider/config substitution. | Defaults == documented values; required-but-missing setting raises (no silent default); llm_required + no provider → LLM_REQUIRED_UNAVAILABLE at run. | P1 | Magic numbers scattered; silent default provider/config; limits only in graph code. |
| 27 | Extended Tool Contracts | §13 (910–931) | `agent/tools/*` / `services/*` | Pydantic In/Out schemas + gated behavior for `get_metric_definition`, `get_schema_context`, `rank_root_causes`, `verify_evidence`, `search_memory`, `create_operation_task`. | Each returns its typed Out or documented error (METRIC_NOT_FOUND, SCHEMA_CONTEXT_MISSING, ATTRIBUTION_COVERAGE_LOW, EVIDENCE_MISSING, MEMORY_READ_FAILED); In rejects extra fields. | P2–P3 | Untyped helper dicts; missing error codes; verify_evidence not enforcing current-run evidence. |

**Note:** Rows 1–25 cover the 25 mandated requirement areas; rows 26–27 are additional foundation/contract gates (config, extended tool contracts) the docs require but that fall outside the mandated list.

---

## Build sequence
- **P1:** rows 1–10, 26 — deps, Makefile, compose, schema, seed, domain, QuerySpec, renderer, guard, repo+audit, config. Gate: `make up && make seed && make test` with test_guard/test_schema/test_seed green; seed idempotent.
- **P2:** rows 12, 16(partial), 17, 18, 27 — services + tools + evidence emission. Gate: test_anomaly/test_attribution/test_tools green without LLM.
- **P3:** rows 11, 13, 14, 15, 16, 19, 20, 24 — graph, nodes, react, trace wiring, reflection, memory, zero-fallback. Gate: test_graph gmv E2E + no_anomaly branch + zero-fallback suite green.
- **P4:** rows 21, 22 — API + UI. Gate: test_api green; UI structural test green.
- **P5:** rows 23, 25, finalize 24 — eval, README/architecture, screenshots. Gate: `make seed && make eval && make test` + unittest discover; 5 cases green incl. no_anomaly no-task; sql_safe=100%.

## Provenance — Codex (5.5 / high) strict review
The original matrix was reviewed adversarially by Codex and returned **FAIL**; no direct concept-substitution, but several proof-tests were too weak to fail against a shortcut. All blocking findings are folded into the rows above:
- Row 19 repair test now enforces full re-entry into the legal ReAct→QuerySpec→SQLRenderer→SQLGuard→Repository path.
- Row 23 eval now requires GT mutation/deletion proof and a real guard-rejected `dangerous_sql_blocked`.
- `metric_rca/services/metric_service.py` added to rows 12/17/18.
- Row 14 gates `METRIC_NOT_FOUND` vs `PARSE_FAILED` separately; rejects re-export shells.
- Row 2 Makefile `api` includes `--reload`; Row 21 endpoint label `{eval_id}`.
- Rows 1/21/22/25 strengthened (clean-venv install, run_rca/repo spies, fake-API-client, doc cross-check).
- Row 20 adds version-conflict resolution test; Row 27 added for extended Tool Contracts.

## Open decisions (do not silently default)
1. Is any MVP run configured `llm_required=true` for the demo, or is deterministic-primary the only path? (Affects whether LLM_REQUIRED_UNAVAILABLE is exercised live.)
2. Eval cases as `cases.jsonl` (roadmap 阶段5) vs `cases.py` (module layout) — jsonl recommended.
3. `scikit-learn` is deferred beyond P1; MVP anomaly math uses deterministic mean/std only.
