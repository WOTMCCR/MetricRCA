# Environment Setup

> Auto-generated by env-preflight skill. Last updated: 2026-06-18T12:05:56+08:00

## Tech Stack

Python application repository. `pyproject.toml` declares the MetricRCA package and test/runtime dependencies.

| Component | Version | Manager | Notes |
|-----------|---------|---------|-------|
| Node.js | v20.19.6 | system / shell PATH | Available for future frontend tooling if needed. |
| Python | 3.12.3 | system / `.venv` | Use `.venv/bin/python`; bare `python` is unavailable unless `.venv/bin` is first in `PATH`. |
| Docker | 29.1.3 | Docker Desktop / WSL | MySQL 8.4 image restored on 2026-06-18. |
| Git | 2.43.0 | system | Repository branch: `main`. |
| ripgrep | 15.1.0 | system | Available and used for search. |

## Environment Constraints

- WSL detected; project is on a native Linux path, not `/mnt/c`.
- Proxy variables are set: `HTTP_PROXY`, `HTTPS_PROXY`, `ALL_PROXY` and lowercase variants point at localhost proxy ports.
- `NO_PROXY` / `no_proxy` include `localhost`, `127.0.0.1`, and `::1`; keep localhost service traffic isolated from external proxy use.
- Project dependencies are installed in project-local `.venv` with `python3 -m venv .venv` and `.venv/bin/python -m pip install -e .`.
- Docker Compose currently relies on the MySQL image-declared anonymous volume for `/var/lib/mysql`; it is Docker-managed, not project-local, and is easy to prune accidentally.
- Current git status includes untracked `docs/` files.

## Check Results

### Latest Preflight: 2026-06-18T12:05:56+08:00

| Category | Check | Status | Notes |
|----------|-------|--------|-------|
| Sandbox | Project dir writable | PASS | Directory permission check reports writable. |
| Sandbox | `/tmp` writable | PASS | Directory permission check reports writable. |
| Sandbox | Home dir writable | PASS | Directory permission check reports writable. |
| Sandbox | Subprocess creation | PASS | Node child process and fork checks succeeded. |
| Network | GitHub | PASS | `curl` short-timeout check succeeded. |
| Network | npm registry | PASS | DNS and `curl` check succeeded. |
| Network | PyPI | PASS | DNS and `curl` check succeeded. |
| Runtime | Node.js | PASS | v20.19.6. |
| Runtime | npm | PASS | 10.8.2. |
| Runtime | Python | PASS | Python 3.12.3 through `.venv`; bare `python` remains unavailable unless `.venv/bin` is first in `PATH`. |
| Runtime | uv | PASS | uv 0.9.18. |
| Runtime | Docker | PASS | Docker 29.1.3 and Compose v2.40.3 available; daemon running. |
| Dependencies | Python venv | PASS | `.venv` exists; `pip check` reports no broken requirements. |
| Dependencies | Frontend deps | PASS | `frontend/node_modules` has 118 top-level entries and 19 `.bin` entries. |
| Workflow | Frontend tests | PASS | 2 files / 12 tests passed with Vitest. |
| Workflow | Makefile | PASS | `make -n test` resolves to `pytest -q`. |
| Docker | Compose MySQL | PASS | `make up` recreated `mysql:8.4`; container is running on port 3307 and regression seed was refreshed after data optimization. |
| Docker | Storage | INFO | `docker system df`: image 1.12GB, anonymous local volume 220.5MB, container 36.86kB. |
| Data | Default regression seed | INFO | Offline deterministic estimate: 37,530 seeded rows, including 26,669 `fact_order` rows. |
| Data | Larger seed profiles | INFO | Offline estimates: acceptance 1,973,331 rows; stress 3,168,889 rows. |
| Project storage | Local repo data | INFO | `.venv` 620M, `frontend/node_modules` 181M, `eval_out` 25M, `artifacts` 1.2M, repo total 849M before restored Docker image. |

### Latest Preflight: 2026-06-08T11:32:02+08:00

| Category | Check | Status | Notes |
|----------|-------|--------|-------|
| Sandbox | Project dir writable | PASS | Write check succeeded. |
| Sandbox | `/tmp` writable | PASS | Temp write check succeeded. |
| Sandbox | Home dir writable | PASS | Home write check succeeded. |
| Sandbox | Subprocess creation | PASS | Node child process check succeeded. |
| Network | GitHub | PASS | `curl` short-timeout check succeeded. |
| Network | npm registry | PASS | `curl` short-timeout check succeeded. |
| Network | PyPI | PASS | `curl` check succeeded after a longer wait; keep timeouts conservative. |
| Runtime | Node.js | PASS | v20.19.6. |
| Runtime | Python | PASS | Python 3.12.3. |
| Workflow | Test runner | SKIP | No application stack or tests exist yet. |
| Workflow | Lint tools | SKIP | No project lint config exists yet. |
| Workflow | Git state | INFO | Branch `main`; `docs/` untracked. |

### Latest Refresh: 2026-06-09T08:43:00+08:00

| Category | Check | Status | Notes |
|----------|-------|--------|-------|
| Runtime | Bare `python` | WARNING | `python` is not on PATH in the default shell. |
| Runtime | Python 3 | PASS | `python3 --version` reports Python 3.12.3. |
| Network | PyPI | PASS | `curl -I --max-time 15 https://pypi.org/simple/pytest/` returned HTTP 200. |
| Dependencies | Local venv | PASS | `.venv` created and editable install completed. |
| Workflow | Seed | PASS | `PATH=.venv/bin:$PATH make seed` completed. |
| Workflow | Test runner | PASS | `PATH=.venv/bin:$PATH make test` completed with 53 passed. |

## Actions Taken

1. Scanned stack marker files and documentation files.
2. Verified basic write access and subprocess behavior.
3. Verified GitHub, npm registry, and PyPI reachability with short-timeout `curl` checks.
4. Created this environment record and project `AGENTS.md` environment summary.
5. Restored local MySQL service with `PATH=.venv/bin:$PATH make up`; this pulled `mysql:8.4` and started `metricrca-mysql-1`.
6. Inspected the restored database schema; all 17 tables exist.
7. Refreshed regression seed after the 2026-06-18 data optimization and verified the 46-case regression slice with `PATH=.venv/bin:$PATH make test`.

## User Actions Required

None.

## Incident Log

No incidents recorded.

### 2026-06-18 Docker cleanup recovery

- User reported Docker space cleanup before this session.
- Initial `docker system df` reported 0 images, 0 containers, 0 local volumes, and 0 build cache.
- `PATH=.venv/bin:$PATH make up` pulled `mysql:8.4` and started `metricrca-mysql-1`.
- Docker storage after restore: image 1.12GB, anonymous local MySQL volume 220.5MB, running container 36.86kB.
- `docker-compose.yml` only bind-mounts `metric_rca/data/schema.sql`; MySQL data is stored in a Docker-managed anonymous volume created from the image `VOLUME`, not in a project-local directory.
- Database schema exists with 17 tables. Regression seed was later refreshed after the data-generation optimization was ready for verification.
- Frontend Vitest smoke passed: 2 test files, 12 tests.
- Python dependency check passed with no broken requirements.

### 2026-06-16 Phase B bounded preflight refresh

- Branch `codex/b-eval-optimization` was created from clean `main`.
- `uv` is available at `/home/ccr/.local/bin/uv`; `uv venv .venv` completed against Python 3.12.3 and warned that `.venv` already existed.
- `uv pip install -e .` refreshed the editable `metric-rca==0.1.0` install.
- PyPI, npm registry, and GitHub were reachable through the current proxy environment.
- `PATH=.venv/bin:$PATH python -m pip check` reported no broken requirements.
- `PATH=.venv/bin:$PATH python -m pytest --version` reported pytest 8.4.2.
- `PATH=.venv/bin:$PATH make -n test` resolved to `pytest -q`.
- `npm run test --prefix frontend -- --run --reporter=dot --passWithNoTests` passed 12 tests.

### 2026-06-15 P8 bounded preflight refresh

- Project, `/tmp`, and home directories are writable by permission checks.
- WSL detected; project remains on a native Linux path.
- Proxy variables remain set for external network access; `NO_PROXY` covers localhost loopback addresses.
- Node subprocess and fork checks passed.
- Python 3.12.3, Node v20.19.6, npm 10.8.2, and GNU Make 4.3 are available.
- `.venv` exists; `PATH=.venv/bin:$PATH python -m pytest --version` reports pytest 8.4.2 and `pip check` reports no broken requirements.
- `frontend/node_modules` and `.bin` exist; frontend vitest starts and reports 11 passed in the preflight smoke.
- PyPI, npm registry, and GitHub are reachable through the current environment.
- `make -n` parses and prints `docker compose up -d mysql`.

### 2026-06-09 Python workflow refresh

- `python` was not available in the default shell, but `python3` was available.
- Created `.venv` and installed the editable project after confirming PyPI access.
- Run project commands as `PATH=.venv/bin:$PATH make seed` and `PATH=.venv/bin:$PATH make test` so Makefile targets that invoke `python` resolve to the virtual environment.
